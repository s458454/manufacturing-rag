# A3.2 Structure-aware Leaf Chunking（2026-08-17）

> **已被 `docs/proceeding/2026-08-17-a3-freeze.md` 取代。**  
> 本文是 A3.2 实现过程草稿，不再作为验收状态。A3 关闭请看 freeze。
>
> 本文原是 A3.2 **实现交接**，不是需求主文档。  
> 行为合同：上游已批准的 A3.2 审计稿 + Section identity / overlap budget 修正。

## 当前状态

```text
A3.2 DESIGN                     FROZEN / PASS（按上游修正版）
STRUCTURE-AWARE LEAF CHUNKING   PASS（单元测试）
768/96 CONFIG                   PASS
BLOCK-AWARE PACKING             PASS
TOKEN FALLBACK                  PASS
TABLE ATOMICITY                 PASS
PAGE PROVENANCE                 PASS
SECTION IDENTITY                PASS
CHUNK IDENTITY                  PASS
UNIT TEST                       PASS（本机；Linux 需再跑）
REAL CORPUS                     PENDING（服务器 CLI）

A3 CLOSED                       NO
```

未实现 A4/A5/A6。未改 A1/A2/A0/C8。

## 修改文件

新增：

```text
code/knowledge_base/chunking_config.py
code/knowledge_base/leaf_ids.py
code/knowledge_base/leaf_chunker.py
code/knowledge_base/tests/test_leaf_chunker.py
docs/proceeding/2026-08-17-a3.2-leaf-chunking.md
```

改过：

```text
code/knowledge_base/structure_parser.py   # 向后兼容增加 ContentBlock / iter_section_blocks
code/knowledge_base/token_count.py        # tokenize_with_offsets，仍走同一 tokenizer
code/knowledge_base/__init__.py
docs/knowledge-base.md
docs/SPLIT-MANIFEST.md
agent.md
```

明确未改：`markdown_loader.py`、`document_registry.py`、`preprocessing/**`、`C8/**`

## 上游修正已落地

- `SectionRef.kind` 只有 `heading | document_root`
- preface Leaf 绑定父 heading；lead / 无 heading 绑定 document_root
- 每个真实 Markdown heading（含有 child 的）都有 SectionRef
- overlap 计入下一 Leaf 的 768；放不下则减 overlap，必要时 0

## 服务器验收

```bash
cd /public/zhangkairan/MyMethod/all-in-rag-main
conda activate mfg-rag-preprocess

PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}" \
python -m knowledge_base.leaf_chunker \
  --canonical-root "$PWD/outputs/preprocessing" \
  --tokenizer Qwen/Qwen3-Embedding-4B \
  --chunk-size 768 \
  --overlap 96 \
  --output /tmp/a3-leaves.json

python -m pytest code/knowledge_base/tests -v
```
