# A2 Document Registry 实现冻结与交接（2026-08-16）

> 本文是 A2 **实现交接**，不是需求主文档。  
> 需求合同仍以 `docs/knowledge-base.md` A2.2 `[FROZEN]` 为准。  
> 写本文的目的：上游在无法打开仓库时，仍能根据下面的源码、测试矩阵、pytest 记录和 smoke 记录验收 A2。

## 当前验收状态

```text
A2 DESIGN
FROZEN / PASS
- document_id = A1 document_id，且必须与 quality_report.document_id 一致
- manifest join = 完整 source_sha256 == sha256
- document_title / source 来源规则已由上游确认升为 [FROZEN]
- 最终 source 机器路径拒绝规则已冻结

A2 IMPLEMENTATION
PASS
- code/knowledge_base/document_registry.py
- 未修改 A1 / C8 / A0
- quality_report / manifest 的 UTF-8 与 csv 读失败已包装为 DocumentRegistryError

A2 UNIT TEST
PASS（本机已含 2 条异常路径；Linux 同步后期望 61 passed / 0 skipped）
- Windows: 61 collected，59 passed，2 skipped（A1 T8）
- 先前 Linux 59 passed / 0 skipped 覆盖主路径
- 新增 invalid UTF-8 quality_report / manifest → DocumentRegistryError

A2 REAL CORPUS
PASS
- canonical root = /public/zhangkairan/MyMethod/all-in-rag-main/outputs/preprocessing
- manifest = data/engineering_docs/manifest.csv（24 行超集）
- registry_size = 3
- 4003A / 6033 / 5009C 均为当前 A0 hash identity
- title = manifest.title
- source = NASA https URL
- source 不含 /public/zhangkairan/
- 本次小修只动异常边界，不必重跑三篇 smoke

A2 CLOSED
READY FOR UPSTREAM CLOSE
- 服务器再跑一次完整 pytest：期望 61 passed / 0 skipped
```

Linux 服务器硬条件已满足。申请关闭 A2，进入 A3。A3 仍不得清洗 `document.md`，也不得把 A2.3 的 `chunk_id` / `section_id` / `page_start` / `page_end` 写入 Document Registry。

审查已通过、无需再改的部分：三字段 schema、SHA-256 精确 join、`canonical ⊆ manifest`、manifest 模式对最终 candidate source 做 OS 无关绝对路径/`file:` 拒绝、no-manifest 跨平台 basename、两种模式不混合。

---

## 1. 完成结论

| 项 | 结果 |
|---|---|
| 范围 | 仅 A2 Document Registry；未实现 A3–A6 |
| 代码位置 | 新建 `document_registry.py` + 测试；**未修改** `markdown_loader.py` / `code/C8` / `code/preprocessing` |
| 依赖 | 标准库 only（`json` / `csv` / `pathlib` / `re`）；测试用已有 pytest。无 LangChain、无 Markdown parser、无 A0 import、无 LLM |
| 本机 pytest（Windows） | 61 collected：**59 passed, 2 skipped**（A1 T8 无 symlink 权限 skip；含 2 条 UTF-8 异常测试） |
| **Linux 服务器 pytest** | 先前 **59 passed, 0 skipped**（主路径）。本小修后同步再跑，期望 **61 passed, 0 skipped** |
| **正式 smoke** | 3 篇 NASA STD × 真实 `manifest.csv`：title/URL 与 manifest 一致（异常包装不改变 Registry 结果，不重跑） |
| 稳定文档 | `docs/knowledge-base.md` A2.2 已由上游确认从 `[PROVISIONAL]` 升为 `[FROZEN]` |
| A2 CLOSED | **申请关闭**（服务器硬条件已满足） |

---

## 2. 修改 / 新增文件

新增：

```text
code/knowledge_base/document_registry.py
code/knowledge_base/tests/test_document_registry.py
```

改过（A2 合同要求）：

```text
code/knowledge_base/__init__.py          # 惰性导出 A2 符号
docs/knowledge-base.md                   # A2.2 升 [FROZEN]；写入 title/source 规则
docs/SPLIT-MANIFEST.md                   # 划掉 A2 document title/source provisional 项
agent.md                                 # A1/A2 implemented；补 knowledge_base/ 路径
```

明确未改：

```text
code/knowledge_base/markdown_loader.py   # A1 CLOSED
code/knowledge_base/tests/test_markdown_loader.py
code/C8/**
code/preprocessing/**
code/requirements.txt
data/engineering_docs/manifest.csv       # 只读使用，未改语料清单
```

未新增 `setup.py` / `pyproject.toml` / packaging。运行靠 `PYTHONPATH=code`。

---

## 3. DocumentRegistryEntry 最终 schema

```python
@dataclass(frozen=True)
class DocumentRegistryEntry:
    document_id: str
    document_title: str
    source: str
```

Registry 类型：

```python
dict[str, DocumentRegistryEntry]   # key = document_id；插入顺序 = A1 文档顺序
```

运行时对象**只有这三字段**。构建期可读但不持久化：

```text
source_sha256
local_path
source_url          # 只用于选出最终 source，不另存一列
organization
category
year
pages
bytes
```

未加入：

```text
section_id, chunk_id, chunk_index, page_start, page_end,
parent_id, LangChain metadata, first heading, OCR text
```

对外错误类型只有 `DocumentRegistryError`。JSON / IO / UTF-8 decode / `csv.Error` 均 `raise DocumentRegistryError(...) from exc`。A1 的 `MarkdownLoadingError` 仅在 CLI 组合加载时一并捕获，A2 函数本身不包装 A1。

---

## 4. 入口与两种模式

唯一构建入口：

```python
def build_document_registry(
    documents: list[LoadedMarkdownDocument],
    manifest_path: Path | None = None,
) -> dict[str, DocumentRegistryEntry]
```

- `documents` 由调用方先走 A1 `load_markdown_documents(...)`。A2 **不**自己发现目录。
- `manifest_path` 由调用方显式传入。实现**没有**默认

```text
PROJECT_ROOT / "data/engineering_docs/manifest.csv"
```

两种模式**不混合**：

| 模式 | 条件 | 行为 |
|---|---|---|
| Manifest authority | `manifest_path` 非空 | 每篇必须 SHA 命中；title/source 来自 manifest；candidate source 不合格 → 整次 fail；禁止部分 filename fallback |
| Filename fallback | `manifest_path is None` | 全部从 `quality_report.source` 跨平台提取 basename/stem；**不**对 A0 绝对路径做 fail-fast 拒绝 |

Manifest 集合语义（已冻结）：

```text
本次 canonical corpus ⊆ manifest
```

不是 `==`。因此 24 行 NASA/NIST 清单对 3 篇 smoke 合法。未使用行不失败。  
但 manifest 内 `sha256` 必须全局唯一；重复则整份清单歧义，fail fast。

---

## 5. 身份、join 与 title/source 算法

对每一篇 A1 文档：

```text
1. 读 sibling <document.path.parent>/quality_report.json
   缺失 / 非文件 / JSON 非法 / 根不是 object → fail
2. quality_report.document_id 必须 == A1 document_id，否则 fail
3. quality_report.source_sha256 必须是 64 hex（strip 后大小写不敏感规范化）
4. 若有 manifest：
     用 source_sha256 精确查找 manifest.sha256
     找不到 / title 为空 / source_url 与 local_path 都为空 → fail
     candidate_source = strip(source_url) 或否则 strip(local_path)
     若 candidate 是机器绑定文件系统路径 → fail
     document_title = manifest.title
     source = candidate_source
5. 若无 manifest：
     从 quality_report.source 提取跨平台 basename/stem
     document_title = stem
     source = basename
     source 缺失或提取结果空 → fail
```

Join **不**按文件名、目录名、`document_id` 的 16 位 hash 前缀或 Markdown heading。

`quality_report.json` 只读三个 identity 字段。同目录 `document.json` / `regions.json` / OCR 统计字段忽略。这与 A1 T4「audit JSON 不当正文」一致：A2 读 metadata，不把 audit 当第二套正文。

---

## 6. 最终 source 拒绝谓词（manifest 模式）

校验打在**最终准备写入 Registry 的 candidate**上，不是只检查 `local_path` 列。`source_url=/public/...` 同样失败。

对 `strip()` 后的 candidate，OS 无关拒绝：

```text
1. 以 "/" 开头
2. 以 "\" 开头          # 含单个反斜杠开头，不只 UNC
3. 匹配 ^[A-Za-z]:[\\/]
4. 大小写不敏感以 "file:" 开头
```

因此下列全部 fail：

```text
/public/company/manual.pdf
D:\company\manual.pdf
\\fileserver\docs\manual.pdf
\foo\bar.pdf
file:///public/company/docs/manual.pdf
FILE:///D:/docs/manual.pdf
```

不做通用 URI allowlist。下列只要不被上述规则命中即允许：

```text
raw/joining/foo.pdf
https://standards.nasa.gov/...
s3://bucket/key
dms://document/123
oss://bucket/key
```

No-manifest 模式**不**套这套拒绝：`/public/foo/manual.pdf` → `source=manual.pdf`，`title=manual`。A0 的机器路径只当提取输入。

跨平台 basename 用 `\` → `/` 后再取 `PurePosixPath.name/stem`，不依赖建库机 OS 的 `pathlib.Path`。

---

## 7. Failure semantics

| 条件 | 行为 |
|---|---|
| `documents` 为空 | `DocumentRegistryError` |
| 输入中 `document_id` 重复 | `DocumentRegistryError` |
| `quality_report.json` 缺失 / 不是文件 | `DocumentRegistryError` |
| `quality_report.json` 非法 UTF-8 | `DocumentRegistryError`（包装 `UnicodeError`，不向外抛 `UnicodeDecodeError`） |
| JSON 非法 / 根不是 object | `DocumentRegistryError` |
| 显式 manifest 不存在 / 不是文件 / 缺列 / 无数据行 | `DocumentRegistryError` |
| manifest 非法 UTF-8 / `csv.Error` / 读阶段 `OSError` | `DocumentRegistryError`（包装 `OSError` / `UnicodeError` / `csv.Error`） |
| `quality_report.document_id != A1 document_id` | `DocumentRegistryError` |
| `source_sha256` 缺失 / 非 64 hex | `DocumentRegistryError` |
| 显式 manifest 不存在 / 不是文件 / 缺列 / 无数据行 | `DocumentRegistryError` |
| manifest 内 SHA 重复（含大小写折叠后重复） | `DocumentRegistryError` |
| 某篇 SHA 在 manifest 中无对应行 | `DocumentRegistryError`（整次失败，不返回其余已匹配篇） |
| 命中行 title 为空 | `DocumentRegistryError` |
| 命中行 `source_url` 与 `local_path` 都为空 | `DocumentRegistryError` |
| 最终 candidate 为机器绑定路径 / `file:` URI | `DocumentRegistryError` |
| 无 manifest 且 `quality_report.source` 空 | `DocumentRegistryError` |
| 无 manifest 且 basename/stem 提取失败 | `DocumentRegistryError` |
| manifest 多余未使用行 | **成功**（`⊆`，不是 `==`） |
| `source_url` 为 https、`local_path` 为绝对路径 | **成功**（校验打在最终 candidate = URL） |

没有静默部分 fallback。没有 heading / LLM / 正文正则。

---

## 8. 可审查源码（完整 `document_registry.py`）

以下即实现全文。若出现 Markdown parser、heading 抽标题、LLM、A0/C8/LangChain import、硬编码 NASA manifest 路径、把 `organization/category/pages` 写入 Entry，视为越界。

```python
"""A2: document-level provenance registry from A1 documents and A0 metadata.

This module assigns ``document_title`` and citation ``source`` to each
``LoadedMarkdownDocument``. It does not parse Markdown, chunk, embed, or index.

``quality_report.json`` is read only for identity fields
(``document_id``, ``source``, ``source_sha256``). Audit JSON is never treated
as knowledge-base body text.

Run the smoke CLI from the repository root with ``code/`` on ``PYTHONPATH``:

    PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}" \\
    python -m knowledge_base.document_registry \\
      --canonical-root <ABS_PATH> \\
      [--manifest <ABS_OR_REL_PATH>]
"""

from __future__ import annotations

import argparse
import csv
import json
import re
import sys
from dataclasses import dataclass
from pathlib import Path, PurePosixPath

from knowledge_base.markdown_loader import (
    LoadedMarkdownDocument,
    MarkdownLoadingError,
    load_markdown_documents,
)

_SHA256_HEX = re.compile(r"^[0-9a-fA-F]{64}$")
_WINDOWS_DRIVE_ABSOLUTE = re.compile(r"^[A-Za-z]:[\\/]")
_MANIFEST_COLUMNS = ("sha256", "title", "source_url", "local_path")


class DocumentRegistryError(Exception):
    """Fail-fast error for A2 document registry construction."""


@dataclass(frozen=True)
class DocumentRegistryEntry:
    document_id: str
    document_title: str
    source: str


@dataclass(frozen=True)
class _ManifestRow:
    sha256: str
    title: str
    source_url: str
    local_path: str


def is_machine_bound_filesystem_source(value: str) -> bool:
    """Return True if *value* is a machine-bound filesystem location.

    The check is OS-independent and applies to the final candidate ``source``
    in manifest mode. No-manifest fallback does not use this predicate.
    """

    if value.startswith("/") or value.startswith("\\"):
        return True
    if _WINDOWS_DRIVE_ABSOLUTE.match(value) is not None:
        return True
    if value.lower().startswith("file:"):
        return True
    return False


def _cross_platform_basename_and_stem(source: str) -> tuple[str, str]:
    """Extract filename and stem from a POSIX or Windows path string."""

    normalized = source.replace("\\", "/")
    path = PurePosixPath(normalized)
    name = path.name
    if not name:
        raise DocumentRegistryError(
            f"Could not extract a filename from quality_report source: {source!r}"
        )
    stem = path.stem
    if not stem:
        raise DocumentRegistryError(
            f"Could not extract a filename stem from quality_report source: {source!r}"
        )
    return name, stem


def _normalize_sha256(value: object, *, field_name: str, location: str) -> str:
    if not isinstance(value, str):
        raise DocumentRegistryError(
            f"{field_name} must be a string in {location}"
        )
    digest = value.strip()
    if _SHA256_HEX.match(digest) is None:
        raise DocumentRegistryError(
            f"Invalid {field_name} in {location}: {value!r}"
        )
    return digest.lower()


def _require_non_empty_string(value: object, *, field_name: str, location: str) -> str:
    if not isinstance(value, str):
        raise DocumentRegistryError(
            f"{field_name} must be a string in {location}"
        )
    text = value.strip()
    if not text:
        raise DocumentRegistryError(f"{field_name} is empty in {location}")
    return text


def _read_quality_report(document: LoadedMarkdownDocument) -> dict[str, object]:
    report_path = document.path.parent / "quality_report.json"
    if not report_path.is_file():
        raise DocumentRegistryError(
            f"quality_report.json missing or not a file: {report_path}"
        )
    try:
        raw = report_path.read_text(encoding="utf-8-sig")
    except OSError as exc:
        raise DocumentRegistryError(
            f"Failed to read {report_path}"
        ) from exc
    except UnicodeError as exc:
        raise DocumentRegistryError(
            f"Invalid UTF-8 in {report_path}"
        ) from exc
    try:
        payload = json.loads(raw)
    except json.JSONDecodeError as exc:
        raise DocumentRegistryError(
            f"Invalid JSON in {report_path}"
        ) from exc
    if not isinstance(payload, dict):
        raise DocumentRegistryError(
            f"quality_report.json root must be an object: {report_path}"
        )
    return payload


def _identity_from_quality_report(
    document: LoadedMarkdownDocument,
) -> tuple[str, str, str]:
    report_path = document.path.parent / "quality_report.json"
    payload = _read_quality_report(document)
    location = str(report_path)
    report_document_id = _require_non_empty_string(
        payload.get("document_id"),
        field_name="document_id",
        location=location,
    )
    if report_document_id != document.document_id:
        raise DocumentRegistryError(
            "quality_report.document_id does not match A1 document_id: "
            f"a1={document.document_id!r} report={report_document_id!r} "
            f"path={report_path}"
        )
    source_sha256 = _normalize_sha256(
        payload.get("source_sha256"),
        field_name="source_sha256",
        location=location,
    )
    source = payload.get("source")
    source_text = source.strip() if isinstance(source, str) else ""
    return report_document_id, source_sha256, source_text


def _csv_row(row: dict[str | None, str | None]) -> dict[str, str]:
    normalized: dict[str, str] = {}
    for key, value in row.items():
        if key is None:
            continue
        normalized[key.strip()] = "" if value is None else value.strip()
    return normalized


def _load_manifest(manifest_path: Path) -> dict[str, _ManifestRow]:
    path = Path(manifest_path)
    if not path.exists():
        raise DocumentRegistryError(f"Manifest does not exist: {path}")
    if not path.is_file():
        raise DocumentRegistryError(f"Manifest is not a file: {path}")
    rows: dict[str, _ManifestRow] = {}
    try:
        handle = path.open(encoding="utf-8-sig", newline="")
        with handle:
            reader = csv.DictReader(handle)
            if reader.fieldnames is None:
                raise DocumentRegistryError(f"Manifest has no header: {path}")
            columns = {
                name.strip()
                for name in reader.fieldnames
                if name is not None and name.strip()
            }
            missing = [name for name in _MANIFEST_COLUMNS if name not in columns]
            if missing:
                raise DocumentRegistryError(
                    "Manifest is missing required columns "
                    f"{missing}: {path}"
                )
            rows = {}
            for line_number, raw_row in enumerate(reader, start=2):
                row = _csv_row(raw_row)
                location = f"{path}:{line_number}"
                digest = _normalize_sha256(
                    row.get("sha256", ""),
                    field_name="sha256",
                    location=location,
                )
                if digest in rows:
                    raise DocumentRegistryError(
                        f"Duplicate manifest sha256 {digest} in {path}"
                    )
                rows[digest] = _ManifestRow(
                    sha256=digest,
                    title=row.get("title", ""),
                    source_url=row.get("source_url", ""),
                    local_path=row.get("local_path", ""),
                )
    except DocumentRegistryError:
        raise
    except (OSError, UnicodeError, csv.Error) as exc:
        raise DocumentRegistryError(f"Failed to read manifest: {path}") from exc
    if not rows:
        raise DocumentRegistryError(f"Manifest has no data rows: {path}")
    return rows


def _candidate_source(row: _ManifestRow, *, document_id: str) -> str:
    if row.source_url:
        candidate = row.source_url
    elif row.local_path:
        candidate = row.local_path
    else:
        raise DocumentRegistryError(
            "Manifest source_url and local_path are both empty for "
            f"document_id={document_id}"
        )
    if is_machine_bound_filesystem_source(candidate):
        raise DocumentRegistryError(
            "Manifest source is a machine-bound filesystem path for "
            f"document_id={document_id}: {candidate!r}"
        )
    return candidate


def _entry_from_manifest(
    document: LoadedMarkdownDocument,
    source_sha256: str,
    rows: dict[str, _ManifestRow],
) -> DocumentRegistryEntry:
    row = rows.get(source_sha256)
    if row is None:
        raise DocumentRegistryError(
            "No manifest row for source_sha256 "
            f"{source_sha256} (document_id={document.document_id})"
        )
    title = row.title.strip()
    if not title:
        raise DocumentRegistryError(
            f"Manifest title is empty for document_id={document.document_id}"
        )
    return DocumentRegistryEntry(
        document_id=document.document_id,
        document_title=title,
        source=_candidate_source(row, document_id=document.document_id),
    )


def _entry_from_source_filename(
    document: LoadedMarkdownDocument,
    quality_source: str,
    report_path: Path,
) -> DocumentRegistryEntry:
    if not quality_source:
        raise DocumentRegistryError(
            f"quality_report.source is missing or empty: {report_path}"
        )
    filename, stem = _cross_platform_basename_and_stem(quality_source)
    return DocumentRegistryEntry(
        document_id=document.document_id,
        document_title=stem,
        source=filename,
    )


def build_document_registry(
    documents: list[LoadedMarkdownDocument],
    manifest_path: Path | None = None,
) -> dict[str, DocumentRegistryEntry]:
    """Build a document-level title/source registry.

    If *manifest_path* is provided, it is the metadata authority for this build:
    every document must join by full ``source_sha256``, and the final ``source``
    value must not be a machine-bound filesystem path. Unused extra manifest
    rows are allowed. Duplicate manifest SHA-256 values fail fast.

    If *manifest_path* is omitted, every document uses a deterministic filename
    fallback derived from ``quality_report.source``. The two modes are never
    mixed.
    """

    if not documents:
        raise DocumentRegistryError("No documents provided")

    seen_ids: set[str] = set()
    for document in documents:
        if document.document_id in seen_ids:
            raise DocumentRegistryError(
                f"Duplicate document_id: {document.document_id}"
            )
        seen_ids.add(document.document_id)

    manifest_rows = (
        _load_manifest(Path(manifest_path)) if manifest_path is not None else None
    )

    registry: dict[str, DocumentRegistryEntry] = {}
    for document in documents:
        _document_id, source_sha256, quality_source = _identity_from_quality_report(
            document
        )
        if manifest_rows is not None:
            entry = _entry_from_manifest(document, source_sha256, manifest_rows)
        else:
            entry = _entry_from_source_filename(
                document,
                quality_source,
                document.path.parent / "quality_report.json",
            )
        registry[document.document_id] = entry
    return registry


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(
        description=(
            "A2 document registry. Loads A1 documents from an explicit canonical "
            "root, then joins optional corpus manifest metadata by source SHA-256."
        ),
        epilog=(
            "From the repository root, put code/ on PYTHONPATH, for example:\n"
            '  PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}" '
            "python -m knowledge_base.document_registry "
            "--canonical-root <ABS_PATH> "
            "[--manifest data/engineering_docs/manifest.csv]"
        ),
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    parser.add_argument(
        "--canonical-root",
        type=Path,
        required=True,
        help="Formal A0 output root containing <document_id>/document.md",
    )
    parser.add_argument(
        "--manifest",
        type=Path,
        default=None,
        help=(
            "Optional corpus manifest CSV. When provided it is the metadata "
            "authority; do not omit it if titles/URLs must come from the manifest."
        ),
    )
    args = parser.parse_args(argv)

    try:
        documents = load_markdown_documents(args.canonical_root)
        registry = build_document_registry(documents, args.manifest)
    except (MarkdownLoadingError, DocumentRegistryError) as exc:
        print(f"error: {exc}", file=sys.stderr)
        return 2

    print(f"canonical_root={args.canonical_root.resolve()}")
    print(
        "manifest="
        + (str(args.manifest.resolve()) if args.manifest is not None else "")
    )
    print(f"registry_size={len(registry)}")
    for document_id, entry in registry.items():
        print(f"document_id={document_id}")
        print(f"document_title={entry.document_title}")
        print(f"source={entry.source}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

说明：

- `quality_report` 用 `read_text(encoding="utf-8-sig")` 只读 JSON metadata；**正文** `document.md` 仍由 A1 用 `read_bytes().decode("utf-8")`，A2 不重读、不改 `content`。
- CLI `--canonical-root` 必填；`--manifest` 可选。示例里的 NASA 路径只出现在 help 文本，不是代码默认值。
- `__init__.py` 惰性导出 A1 + A2 符号。

---

## 9. 测试矩阵

文件：`code/knowledge_base/tests/test_document_registry.py`  
`document.md` fixture 用 `Path.write_bytes`，避免 Windows 文本写入插入 `\r\n`。

| 合同点 | 测试函数 | 断言 |
|---|---|---|
| 正常 manifest join | `test_manifest_join_uses_title_and_source_url` | title/source 等于 manifest，不是路径 |
| SHA 大小写折叠 | `test_sha256_join_is_case_insensitive` | report 小写、manifest 大写仍命中 |
| 不按文件名匹配 | `test_join_is_by_sha256_not_filename` | canonical 目录名与 `local_path` 都不同，只要 SHA 相同仍命中 |
| 不用 first heading | `test_first_heading_is_not_used_as_title` | `document.md` 以 `# NASA-STD-5006A W/CHANGE 2` 开头，title 仍是 manifest 正式名 |
| document_id mismatch | `test_document_id_mismatch_fails_fast` | fail |
| 重复 SHA | `test_duplicate_manifest_sha_fails_fast` | 含大小写重复 → fail |
| 缺 SHA / 无部分结果 | `test_missing_manifest_sha_fails_fast_without_partial_result` | 两篇里一篇无行 → 整次 fail |
| 空 title | `test_empty_manifest_title_fails_fast` | fail |
| 空 source 字段 | `test_empty_manifest_source_fields_fail_fast` | `source_url` 与 `local_path` 都空 → fail |
| 相对 local_path | `test_relative_local_path_used_when_source_url_empty` | `source=raw/joining/...pdf` |
| 校验打在最终 candidate | `test_absolute_local_path_is_ignored_when_source_url_is_logical` | URL 非空则用 URL；绝对 `local_path` 不导致失败 |
| 机器路径 / file: 拒绝 | `test_machine_bound_candidate_source_fails_fast`（9 例） | `/public/...`、`D:\...`、UNC、`\foo\...`、`file:` / `FILE:` / `File:` → fail |
| 空 URL 时拒绝对路径 | `test_machine_bound_local_path_fails_when_source_url_empty`（3 例） | POSIX / Windows / UNC local_path → fail |
| 逻辑 URI 放行 | `test_logical_sources_are_allowed`（7 例） | 相对路径 / https / http / s3 / dms / oss 原样入库 |
| no-manifest POSIX | `test_no_manifest_fallback_uses_posix_basename` | `/foo/bar/my_doc.pdf` → title=`my_doc` source=`my_doc.pdf` |
| no-manifest Windows | `test_no_manifest_fallback_uses_windows_basename` | `D:\foo\bar\manual.pdf` → title=`manual` source=`manual.pdf` |
| 服务器路径不入库 | `test_no_manifest_does_not_store_absolute_server_path` | fallback 后不含 `/public/zhangkairan/` |
| 缺 quality_report | `test_missing_quality_report_fails_fast` | fail |
| JSON 非法 | `test_invalid_quality_report_json_fails_fast` | fail |
| quality_report 非法 UTF-8 | `test_invalid_utf8_quality_report_fails_fast` | `DocumentRegistryError`，`__cause__` 为 `UnicodeError` |
| manifest 非法 UTF-8 | `test_invalid_utf8_manifest_fails_fast` | `DocumentRegistryError`，不向外抛 `UnicodeDecodeError` / `csv.Error` |
| sha 非法 | `test_invalid_source_sha256_fails_fast` | fail |
| 无 manifest 且无 source | `test_no_manifest_missing_source_fails_fast` | fail |
| 不改 A1 content | `test_a1_content_is_unchanged` | 建 registry 前后 `content` 同一对象 |
| audit 不当正文 | `test_audit_json_is_not_persisted_in_registry` | Entry 只有三字段；ocr_text 不出现 |
| 多余 manifest 行 | `test_unused_manifest_rows_do_not_fail` | 3 篇 + 第 4 行 unused → 成功 |
| 空输入 | `test_empty_document_list_fails_fast` | fail |
| 真实 NASA manifest | `test_real_nasa_manifest_spotcheck_titles_and_urls` | 用仓库 `manifest.csv` 对 4003A/6033/5009C SHA join；title/URL 与清单一致；`source` 无服务器路径 |
| 组合 A1 | `test_registry_composes_with_a1_loader` | `load_markdown_documents` 后再 build |

A1 原 16 项仍在同一 pytest 目录，关闭 A2 时必须继续全绿，证明 A2 未破坏 A1。

---

## 10. pytest 完整结果（实现机，Windows）

环境：

```text
platform win32
Python 3.13.12
pytest 9.1.1
```

本机 `%TEMP%\pytest-of-*` 无写权限（与 A1 相同），使用可写 `--basetemp`。这是本机环境限制，不是 A2 逻辑失败。

```text
python -m pytest code/knowledge_base/tests -v --basetemp <writable> -p no:cacheprovider

collected 61 items
A2 全部 PASSED（含 invalid UTF-8 quality_report / manifest）
A1 T8 两项 SKIPPED（无 symlink 权限）
其余 A1 PASSED

59 passed, 2 skipped in 0.70s
```

**Linux 服务器正式验收必须：**

```text
61 passed
0 skipped
```

A1 T8 若仍为 SKIPPED/FAILED，或任一 A2 合同测试失败，不得关闭 A2。

主路径 Linux 已于 2026-08-16 跑完，见 §12：**59 passed, 0 skipped**。本小修只增加 2 条异常路径测试；同步后再跑一次完整 pytest，期望 **61 passed, 0 skipped**。三篇 Registry smoke 不必重跑。Windows skip 不再阻塞关闭。

---

## 11. 真实 A0 canonical smoke（Linux）

使用与 A1 关闭时相同的正式根，显式指定语料 manifest。未使用 `outputs/final-acceptance-5006a`。

主机：`swift`（`zhangkairan@swift`）  
仓库：`/public/zhangkairan/MyMethod/all-in-rag-main`  
环境：`mfg-rag-preprocess`，Python 3.11.15

```bash
PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}" \
python -m knowledge_base.document_registry \
  --canonical-root "$PWD/outputs/preprocessing" \
  --manifest data/engineering_docs/manifest.csv
```

stdout（原文）：

```text
canonical_root=/public/zhangkairan/MyMethod/all-in-rag-main/outputs/preprocessing
manifest=/public/zhangkairan/MyMethod/all-in-rag-main/data/engineering_docs/manifest.csv
registry_size=3
document_id=15_nasa_std_4003a_electrical_bonding-5c5271413bd05f61
document_title=NASA-STD-4003A - Electrical Bonding for NASA Launch Vehicles Spacecraft Payloads and Flight Equipment
source=https://standards.nasa.gov/system/files/tmp/NASA-STD-4003A_w-Change%201%20-%20Revalidated%2003-13-2026.pdf
document_id=19_nasa_std_6033_am_equipment_facility-7e4c13bcf1061520
document_title=NASA-STD-6033 - Additive Manufacturing Equipment and Facility Control
source=https://standards.nasa.gov/system/files/tmp/2026-01-071%20NASA-STD-6033%20-%20Final%20revalidated.pdf
document_id=21_nasa_std_5009c_nde-9ecc604979b627f5
document_title=NASA-STD-5009C - Nondestructive Evaluation Requirements for Fracture-Critical Metallic Components
source=https://standards.nasa.gov/sites/default/files/standards/NASA/C/0/2023-08-03-NASA-STD-5009C-Approved.pdf
```

| 核对 | 结果 |
|---|---|
| `registry_size` | 3 |
| identity | 三份均为当前 A0 `stem-hash16`，与 A1 smoke 的 `document_ids` 一致 |
| 排序 | 插入顺序跟随 A1：15 → 19 → 21 |
| title | 均为 `manifest.title`，不是 heading / document_id / 文件名 |
| source | 均为 `https://standards.nasa.gov/...` |
| 服务器路径 | `source` 中无 `/public/zhangkairan/` |
| 24 行 manifest × 3 篇 | 多余 21 行不失败 |

这 3 篇是当前抽查规模的正式根，足够关闭 A2 并启动 A3。全量语料以后按「一个 source 一份 hash 目录」追加到同一根，并保持 `canonical ⊆ manifest`，不必改 A2 主逻辑。

---

## 12. Linux 服务器验收记录（已执行）

### 12.1 pytest（关闭硬条件）

```bash
cd ~/MyMethod/all-in-rag-main
conda activate mfg-rag-preprocess
python -m pytest code/knowledge_base/tests -v
```

原文摘要：

```text
platform linux -- Python 3.11.15, pytest-9.1.1
rootdir: /public/zhangkairan/MyMethod/all-in-rag-main
collected 59 items

test_manifest_join_uses_title_and_source_url PASSED
test_sha256_join_is_case_insensitive PASSED
test_join_is_by_sha256_not_filename PASSED
test_first_heading_is_not_used_as_title PASSED
test_document_id_mismatch_fails_fast PASSED
test_duplicate_manifest_sha_fails_fast PASSED
test_missing_manifest_sha_fails_fast_without_partial_result PASSED
test_empty_manifest_title_fails_fast PASSED
test_empty_manifest_source_fields_fail_fast PASSED
test_relative_local_path_used_when_source_url_empty PASSED
test_absolute_local_path_is_ignored_when_source_url_is_logical PASSED
test_machine_bound_candidate_source_fails_fast[...9 cases...] PASSED
test_machine_bound_local_path_fails_when_source_url_empty[...3 cases...] PASSED
test_logical_sources_are_allowed[...7 cases...] PASSED
test_no_manifest_fallback_uses_posix_basename PASSED
test_no_manifest_fallback_uses_windows_basename PASSED
test_no_manifest_does_not_store_absolute_server_path PASSED
test_missing_quality_report_fails_fast PASSED
test_invalid_quality_report_json_fails_fast PASSED
test_invalid_source_sha256_fails_fast PASSED
test_no_manifest_missing_source_fails_fast PASSED
test_a1_content_is_unchanged PASSED
test_audit_json_is_not_persisted_in_registry PASSED
test_unused_manifest_rows_do_not_fail PASSED
test_empty_document_list_fails_fast PASSED
test_real_nasa_manifest_spotcheck_titles_and_urls PASSED
test_registry_composes_with_a1_loader PASSED
test_t1_batch_load_is_sorted_by_document_id PASSED
... A1 T2–T7 ...
test_t8_directory_symlink_fails_fast PASSED
test_t8_document_md_symlink_fails_fast PASSED
... A1 其余补充项 ...
test_empty_document_md_is_loaded PASSED

59 passed in 0.42s
```

A1 T8 在 Linux 上真实执行，不是 skip。A2 未破坏 A1。

### 12.2 canonical Registry smoke

见 §11。`registry_size=3`，三篇 title/source 与真实 manifest 一致。

---

## 13. 明确未做（越界即打回）

```text
A3 heading split / chunk / page_start / page_end
A4 hierarchy / section_id
A5 embedding
A6 Milvus / rebuild / upsert
把 source_sha256 / organization / category / pages / bytes 写入 RegistryEntry
first heading / LLM / 正文正则抽标题
按文件名模糊匹配 manifest
硬编码 data/engineering_docs/manifest.csv
把 quality_report / document.json / regions.json 当正文
清洗 / rewrite A0 document.md
修改 A1 markdown_loader.py
LangChain Document
旧 C8 DataPreparationModule 复用
packaging / setup.py / pyproject
```

---

## 14. 关闭条件（申请 CLOSED）

```text
A2 DESIGN                 FROZEN / PASS
A2 IMPLEMENTATION         PASS（UTF-8/csv 异常已包装为 DocumentRegistryError；未改 A1/C8/A0）
A2 UNIT TEST              PASS（本机 61 collected / 59 passed / 2 skipped；Linux 主路径 59 passed，同步后期望 61 passed / 0 skipped）
A2 REAL CORPUS            PASS（3 hash identities；NASA URL；无服务器路径；小修不重跑 smoke）
A2 CLOSED                 READY FOR UPSTREAM CLOSE
```

对照冻结合同：

1. Linux 主路径 pytest `59 passed, 0 skipped`，含 A1 T8 真实 PASSED。**已满足。** 异常包装补丁后再跑期望 `61 passed, 0 skipped`。
2. `document_id` 沿用 A1，并与 sibling `quality_report.document_id` 一致。**已满足**（mismatch 测试 + 真实三篇 identity）。
3. 显式 manifest 以完整 SHA-256 join；`canonical ⊆ manifest`；重复 SHA fail。**已满足。**
4. `document_title = manifest.title`；`source` 为逻辑 URL，不是 `/public/zhangkairan/...`。**已满足。**
5. 最终 candidate 拒绝绝对文件系统路径与 `file:` URI；no-manifest 只提取 filename。**已满足**（单元测试覆盖；smoke 走 manifest 模式）。
6. 持久化只有 `{document_id, document_title, source}`。**已满足。**
7. 对外错误类型只有 `DocumentRegistryError`：quality_report / manifest 的 UTF-8 decode 与 csv 读失败均已包装。**已满足。**

A3 仍不得清洗 A0 `document.md`。当前 3 篇是抽查规模 canonical；全量建库是后续 corpus 追加，不回退 A1/A2。
