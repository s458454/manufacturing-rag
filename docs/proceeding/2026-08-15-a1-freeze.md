# A1 Markdown Loading 实现冻结与交接（2026-08-15）

> 本文是 A1 **实现交接**，不是需求主文档。  
> 需求合同仍以 `docs/knowledge-base.md` A1 `[FROZEN]` 为准。  
> 写本文的目的：上游在无法打开仓库时，仍能根据下面的源码、测试矩阵、pytest 记录和 smoke 记录验收 A1。

## 当前验收状态

```text
A1 DESIGN
PASS

A1 IMPLEMENTATION
PASS
- wrap canonical-root enumeration OSError as MarkdownLoadingError

A1 UNIT TEST
PASS
- Linux: 16 passed, 0 skipped
- T8 directory symlink PASSED
- T8 document.md symlink PASSED
- enumeration OSError wrap PASSED

A1 REAL CORPUS
PASS
- canonical root = /public/zhangkairan/MyMethod/all-in-rag-main/outputs/preprocessing
- discovered = loaded = 3
- all current A0 hash identities
- no legacy un-hashed duplicate publication
- lossless_check ok

A1 CLOSED
READY FOR UPSTREAM CLOSE
```

Linux 服务器硬条件已满足。正式 canonical root 已换成当前冻结 A0 抽查发布的 3 份 NASA STD（4003A / 6033 / 5009C），不再使用旧铝手册双目录。申请关闭 A1，进入 A2。A2 仍不得清洗 `document.md`。

审查已通过、无需再改的部分：三字段 schema、一级 discovery、`read_bytes().decode("utf-8")`、无 parser/metadata/C8、空 `document.md` 由 A1 放行、CLI `--canonical-root` 必填。

---

## 1. 完成结论

| 项 | 结果 |
|---|---|
| 范围 | 仅 A1 lossless loading；未实现 A2–A6 |
| 代码位置 | 新建 `code/knowledge_base/`，**未修改** `code/C8` |
| 依赖 | 标准库 only（测试用已有 pytest）；无 LangChain、无 Markdown parser、无 A0 import |
| 本机 pytest（Windows） | 16 collected：**14 passed, 2 skipped**（T8 无 symlink 权限 skip） |
| **Linux 服务器 pytest** | **16 passed, 0 skipped**（T8 两项均 PASSED） |
| **正式 smoke** | `outputs/preprocessing`：`discovered = loaded = 3`；三份均为当前 A0 `stem-hash16`；`lossless_check=ok` |
| 未使用 | `outputs/final-acceptance-5006a` |
| 历史插曲 | 旧铝手册双目录根曾 load 成功后被删；已用冻结 A0 的 4003A/6033/5009C 替换 |
| A1 CLOSED | **申请关闭**（服务器硬条件已满足） |

---

## 2. 修改 / 新增文件

只新增，不改旧链路：

```text
code/knowledge_base/__init__.py
code/knowledge_base/markdown_loader.py
code/knowledge_base/tests/__init__.py
code/knowledge_base/tests/test_markdown_loader.py
```

明确未改：

```text
code/C8/**
code/preprocessing/**
code/requirements.txt
docs/knowledge-base.md          # 需求已冻结，本任务不改合同
```

未新增 `setup.py` / `pyproject.toml` / packaging。运行靠 `PYTHONPATH=code`。

---

## 3. LoadedMarkdownDocument 最终 schema

```python
@dataclass(frozen=True)
class LoadedMarkdownDocument:
    document_id: str   # document.md 的直接父目录名 = A0 已发布的 stable identity
    content: str       # document.md 完整 UTF-8 解码结果，无任何规范化
    path: Path         # document.md.resolve()；仅运行时审计/报错，不是未来 Milvus 字段
```

A1 运行时对象**只有这三字段**。未加入：

```text
document_title, source, category, domain, material, year,
organization, document_type, difficulty, parent_id, section_id,
chunk_id, chunk_index, page_start, page_end, content hash,
LangChain metadata
```

`document_id` **不**从 PDF hash 重算，**不**读 `document.json`。

对外错误类型只有 `MarkdownLoadingError`。OSError / `UnicodeDecodeError` 均 `raise MarkdownLoadingError(...) from exc`。

---

## 4. Discovery 算法

唯一入口：

```python
def load_markdown_documents(canonical_root: Path) -> list[LoadedMarkdownDocument]
```

`canonical_root` 必须由调用方显式传入。实现**没有**默认根，也**没有**：

```text
rglob / glob("**") / glob("outputs/**/document.md")
猜测最新 outputs、acceptance、smoke
按路径字符串过滤 "acceptance" / "smoke"
```

算法（与源码一致）：

```text
1. root = Path(canonical_root)
2. try:
     不存在            → MarkdownLoadingError
     不是目录          → MarkdownLoadingError
     for child in root.iterdir():          # 只扫直接一级
       若不是目录: continue
       若 child.is_symlink():              # 一级文档目录不接受 symlink
           raise MarkdownLoadingError      # fail fast，不 skip
       candidate = child / "document.md"
       若 candidate.is_symlink():          # document.md 不接受 symlink
           raise MarkdownLoadingError
       若 candidate 不是普通文件: continue
       discovered.append(candidate)
   except MarkdownLoadingError: 原样抛出
   except OSError: 包装为 MarkdownLoadingError("Failed to enumerate canonical root")
3. discovered 为空    → MarkdownLoadingError
4. sort by parent.name（document_id，Unicode code point 升序）
5. 按序 read_bytes().decode("utf-8")；任一失败 → 整次 load 失败
```

因此自然不进入：

```text
<root>/.failed/<id>.<uuid>/document.md
<root>/.staging/<id>.<uuid>/document.md
<root>/nested/other/document.md
<root>/document.md                  # 根上直接放的文件，没有 document_id 目录
<root>/<id>/document.json 等 audit
```

隔离靠「调用方给对根 + 不递归 + 拒绝 symlink」，**不**靠文件名关键词特判。

---

## 5. Lossless 合同

读取固定为：

```python
raw = path.read_bytes()
content = raw.decode("utf-8")   # strict；无 errors="replace"
```

禁止项（源码中均不存在）：

```text
Path.read_text()          # 避免 universal newline 把 \r\n 变成 \n
.decode("utf-8-sig")      # 避免剥 BOM
.strip() / lstrip / rstrip
删除 <!-- PDF page N -->
Markdown parser / HTML 转换 / 表格重排 / heading 拼进 content
```

正确性断言：

```python
loaded.content == 源文件 UTF-8 文本
loaded.content.encode("utf-8") == 源文件字节
```

page marker、heading、空行、list、caption、admitted pipe table 全部原样保留。  
page marker 是否最终进入 retrieval text 仍是 A3 `[PROVISIONAL]`；A1 不预删。

---

## 6. Failure semantics

| 条件 | 行为 |
|---|---|
| root 不存在 | `MarkdownLoadingError` |
| root 不是目录 | `MarkdownLoadingError` |
| 列目录 / discovery 阶段 `OSError`（含 `PermissionError`） | `MarkdownLoadingError`（`from` 原始异常；**不**把 `OSError` 泄露给调用方） |
| 一级找不到任何 `*/document.md` | `MarkdownLoadingError` |
| 一级文档目录是 symlink | `MarkdownLoadingError`（不 skip） |
| `document.md` 是 symlink | `MarkdownLoadingError`（不 skip） |
| 已发现文件读失败 | `MarkdownLoadingError` |
| UTF-8 严格解码失败 | `MarkdownLoadingError` |
| 同一次 load 中后一篇失败 | 整体失败，不返回部分列表 |
| 某一级子目录没有 `document.md` | **忽略**（`.locks` / 空目录） |
| `document.md` 是空文件 | **成功**，`content == ""` |

没有旧 C8 的 `except Exception: logger.warning; continue`。

---

## 7. 可审查源码（完整 `markdown_loader.py`）

以下即实现全文。若出现 parser、业务 metadata、C8/LangChain import，视为越界。

```python
"""A1: lossless Markdown loading from an explicit A0 canonical publish root.

This module only discovers and reads formal ``<canonical_root>/<document_id>/document.md``.
It does not parse Markdown, enrich metadata, chunk, embed, or index.

Run the smoke CLI from the repository root with ``code/`` on ``PYTHONPATH``:

    PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}" \\
    python -m knowledge_base.markdown_loader \\
      --canonical-root <ABS_PATH>
"""

from __future__ import annotations

import argparse
import sys
from dataclasses import dataclass
from pathlib import Path


class MarkdownLoadingError(Exception):
    """Fail-fast error for A1 Markdown loading."""


@dataclass(frozen=True)
class LoadedMarkdownDocument:
    document_id: str
    content: str
    path: Path


def load_markdown_documents(canonical_root: Path) -> list[LoadedMarkdownDocument]:
    root = Path(canonical_root)
    discovered: list[Path] = []
    try:
        if not root.exists():
            raise MarkdownLoadingError(f"Canonical root does not exist: {root}")
        if not root.is_dir():
            raise MarkdownLoadingError(f"Canonical root is not a directory: {root}")
        for child in root.iterdir():
            if not child.is_dir():
                continue
            if child.is_symlink():
                raise MarkdownLoadingError(
                    f"Refusing to load a symlinked document directory: {child}"
                )
            candidate = child / "document.md"
            if candidate.is_symlink():
                raise MarkdownLoadingError(
                    f"Refusing to load a symlinked document.md: {candidate}"
                )
            if not candidate.is_file():
                continue
            discovered.append(candidate)
    except MarkdownLoadingError:
        raise
    except OSError as exc:
        raise MarkdownLoadingError(
            f"Failed to enumerate canonical root: {root}"
        ) from exc

    if not discovered:
        raise MarkdownLoadingError(
            "No document.md found under direct children of: "
            f"{root}"
        )

    discovered.sort(key=lambda path: path.parent.name)

    loaded: list[LoadedMarkdownDocument] = []
    for path in discovered:
        try:
            raw = path.read_bytes()
        except OSError as exc:
            raise MarkdownLoadingError(f"Failed to read {path}") from exc
        try:
            content = raw.decode("utf-8")
        except UnicodeDecodeError as exc:
            raise MarkdownLoadingError(f"Invalid UTF-8 in {path}") from exc
        loaded.append(
            LoadedMarkdownDocument(
                document_id=path.parent.name,
                content=content,
                path=path.resolve(),
            )
        )
    return loaded


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(
        description=(
            "A1 lossless Markdown loader. "
            "Requires an explicit A0 canonical publish root; "
            "does not guess outputs, smoke, or acceptance directories."
        ),
        epilog=(
            "From the repository root, put code/ on PYTHONPATH, for example:\n"
            '  PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}" '
            "python -m knowledge_base.markdown_loader "
            "--canonical-root <ABS_PATH>"
        ),
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    parser.add_argument(
        "--canonical-root",
        type=Path,
        required=True,
        help="Formal A0 output root containing <document_id>/document.md",
    )
    args = parser.parse_args(argv)

    try:
        documents = load_markdown_documents(args.canonical_root)
    except MarkdownLoadingError as exc:
        print(f"error: {exc}", file=sys.stderr)
        return 2

    first = documents[0]
    lossless = first.content.encode("utf-8") == first.path.read_bytes()
    print(f"canonical_root={args.canonical_root.resolve()}")
    print(f"discovered={len(documents)}")
    print(f"loaded={len(documents)}")
    print(f"document_ids={','.join(doc.document_id for doc in documents)}")
    print(f"lossless_check={first.document_id}:{'ok' if lossless else 'fail'}")
    return 0 if lossless else 1


if __name__ == "__main__":
    raise SystemExit(main())
```

`__init__.py` 用 `__getattr__` 惰性再导出上述三个符号，避免 `python -m knowledge_base.markdown_loader` 时包初始化先 import 同名模块。

CLI `--canonical-root` **必填**，无默认值。

---

## 8. 测试矩阵

文件：`code/knowledge_base/tests/test_markdown_loader.py`  
fixture 一律 `Path.write_bytes`，避免 Windows 文本写入插入 `\r\n`。

| ID | 测试函数 | 断言 |
|---|---|---|
| T1 | `test_t1_batch_load_is_sorted_by_document_id` | 先写 `doc-b` 再写 `doc-a`，输出仍为 `doc-a, doc-b` |
| T2 | `test_t2_lossless_markdown_is_byte_identical` | heading / `<!-- PDF page N -->` / list / caption / table / 中英混排 / 文首空行；`content == original` 且 `encode("utf-8") == 源字节` |
| T3 | `test_t3_does_not_recurse_into_nested_document_md` | `nested/other/document.md` 不加载 |
| T4 | `test_t4_does_not_load_a0_audit_json` | 同目录有 `document.json` / `regions.json` / `quality_report.json` / `tables/*.json`，只加载 `document.md` |
| T5 | `test_t5_does_not_enter_failed_or_staging_trees` | `.failed/failed-doc/document.md` 与 `.staging/staging-doc/document.md` 不加载；代码无 `"failed" in path` 特判 |
| T6 | `test_t6_invalid_utf8_fails_the_entire_load` | 非法 UTF-8 → `MarkdownLoadingError` |
| T6 | `test_t6_invalid_utf8_does_not_return_partial_corpus` | 第一篇合法、第二篇非法 → 整次失败 |
| T7 | `test_t7_empty_corpus_fails_fast` | 空根 → error |
| T7 | `test_t7_nested_only_corpus_is_empty` | 只有 `.failed/.../document.md` → error |
| T8 | `test_t8_directory_symlink_fails_fast` | 一级目录 symlink → error（有合法 sibling 也不 skip） |
| T8 | `test_t8_document_md_symlink_fails_fast` | `document.md` symlink → error |
| 补充 | `test_missing_canonical_root_fails_fast` | 根不存在 → error |
| 补充 | `test_canonical_root_enumeration_oserror_is_wrapped` | mock `Path.iterdir` 抛 `PermissionError` → `MarkdownLoadingError`，`__cause__` 仍为 `PermissionError` |
| 补充 | `test_canonical_root_file_fails_fast` | 根是文件 → error |
| 补充 | `test_root_level_document_md_is_ignored` | 根上直接放 `document.md` 不加载 |
| 补充 | `test_empty_document_md_is_loaded` | 空文件成功，`content == ""` |

T2 使用的原文（完整相等，不是关键词包含）：

```markdown
<!-- PDF page 25 -->

# 4 Welding
## 4.2 Welding Requirements

Paragraph A.

- item 1
- item 2

Figure 4—Fillet Welds

| A | B |
|---|---|
| 1 | 2 |

<!-- PDF page 26 -->

中英混排: welding 要求
```

（fixture 在文首另有一个空行，用于证明未 `.strip()`。）

---

## 9. pytest 完整结果（实现机，Windows）

环境：

```text
platform win32
Python 3.13.12
pytest 9.1.1
```

本机 `%TEMP%\pytest-of-*` 无写权限（A0 proceeding 记载过同一类问题），因此使用可写 `--basetemp`。这是本机环境限制，不是 A1 逻辑失败。Linux 服务器一般可直接跑第 11 节命令。

```text
python -m pytest code/knowledge_base/tests -v --basetemp <writable> -p no:cacheprovider

collected 16 items

test_t1_batch_load_is_sorted_by_document_id PASSED
test_t2_lossless_markdown_is_byte_identical PASSED
test_t3_does_not_recurse_into_nested_document_md PASSED
test_t4_does_not_load_a0_audit_json PASSED
test_t5_does_not_enter_failed_or_staging_trees PASSED
test_t6_invalid_utf8_fails_the_entire_load PASSED
test_t6_invalid_utf8_does_not_return_partial_corpus PASSED
test_t7_empty_corpus_fails_fast PASSED
test_t7_nested_only_corpus_is_empty PASSED
test_t8_directory_symlink_fails_fast SKIPPED
test_t8_document_md_symlink_fails_fast SKIPPED
test_missing_canonical_root_fails_fast PASSED
test_canonical_root_enumeration_oserror_is_wrapped PASSED
test_canonical_root_file_fails_fast PASSED
test_root_level_document_md_is_ignored PASSED
test_empty_document_md_is_loaded PASSED

14 passed, 2 skipped in 0.15s
```

T8 skip 原因（Windows 无 symlink 创建权限，审查时已允许 POSIX 条件执行）：

```text
WinError 1314 客户端没有所需的特权
```

enumeration wrap 测试用 `unittest.mock` 让 `Path.iterdir` 抛 `PermissionError`，不依赖真实 chmod，Windows/Linux 都应 PASSED。

**Linux 服务器正式验收必须：**

```text
16 passed
0 skipped
```

T8 两项若仍为 SKIPPED 或 FAILED，不能算验收完成，不得关闭 A1。

Linux 服务器已于 2026-08-15 跑完，见 §11：**16 passed, 0 skipped**。Windows skip 不再阻塞关闭。

---

## 10. 真实 A0 canonical smoke

**未使用** `outputs/final-acceptance-5006a`。

### 10.1 历史：旧铝手册根（已作废，不得进 A2）

实现机曾对旧 `outputs/preprocessing` smoke：`discovered = loaded = 2`，但同一铝手册同时存在无-hash 与 hash 两份目录。A1 两个都加载是正确行为；该根不能作为 A2 输入。该目录随后从服务器消失。未在 A1 增加按 hash 去重。

### 10.2 正式：当前冻结 A0 的 3 份 NASA STD（2026-08-15，Linux）

正式 canonical root 已换成当前冻结 A0 抽查发布产物（4003A / 6033 / 5009C 成功目录拷入 `outputs/preprocessing` 一级，不套 spotcheck 子层，不拷 `.failed/.staging`）。

主机：`swift`（`zhangkairan@swift`）  
仓库：`/public/zhangkairan/MyMethod/all-in-rag-main`  
环境：`mfg-rag-preprocess`，Python 3.11.15

```bash
export PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}"
python -m knowledge_base.markdown_loader \
  --canonical-root "$PWD/outputs/preprocessing"
```

stdout（原文）：

```text
canonical_root=/public/zhangkairan/MyMethod/all-in-rag-main/outputs/preprocessing
discovered=3
loaded=3
document_ids=15_nasa_std_4003a_electrical_bonding-5c5271413bd05f61,19_nasa_std_6033_am_equipment_facility-7e4c13bcf1061520,21_nasa_std_5009c_nde-9ecc604979b627f5
lossless_check=15_nasa_std_4003a_electrical_bonding-5c5271413bd05f61:ok
```

| 核对 | 结果 |
|---|---|
| `discovered == loaded` | 3 = 3 |
| identity | 三份均为当前 A0 `stem-hash16` |
| 排序 | `document_id` 升序：15 → 19 → 21 |
| legacy 无-hash 铝手册 | 不在此根 |
| 5006A acceptance | 未使用 |

这 3 篇是当前抽查规模的正式根，足够关闭 A1 并启动 A2。全量语料以后按「一个 source 一份 hash 目录」追加到同一根，不必改 A1。

---

## 11. Linux 服务器验收记录（已执行）

### 11.1 pytest（关闭硬条件）

```bash
cd ~/MyMethod/all-in-rag-main
conda activate mfg-rag-preprocess
export PYTHONPATH="$PWD/code${PYTHONPATH:+:$PYTHONPATH}"
python -c "from knowledge_base.markdown_loader import load_markdown_documents; print('ok')"
python -m pytest code/knowledge_base/tests -v
```

原文摘要：

```text
ok
platform linux -- Python 3.11.15, pytest-9.1.1
rootdir: /public/zhangkairan/MyMethod/all-in-rag-main
collected 16 items

test_t1_batch_load_is_sorted_by_document_id PASSED
test_t2_lossless_markdown_is_byte_identical PASSED
test_t3_does_not_recurse_into_nested_document_md PASSED
test_t4_does_not_load_a0_audit_json PASSED
test_t5_does_not_enter_failed_or_staging_trees PASSED
test_t6_invalid_utf8_fails_the_entire_load PASSED
test_t6_invalid_utf8_does_not_return_partial_corpus PASSED
test_t7_empty_corpus_fails_fast PASSED
test_t7_nested_only_corpus_is_empty PASSED
test_t8_directory_symlink_fails_fast PASSED
test_t8_document_md_symlink_fails_fast PASSED
test_missing_canonical_root_fails_fast PASSED
test_canonical_root_enumeration_oserror_is_wrapped PASSED
test_canonical_root_file_fails_fast PASSED
test_root_level_document_md_is_ignored PASSED
test_empty_document_md_is_loaded PASSED

16 passed in 0.17s
```

T8 在 Linux 上真实执行，不是 skip。

### 11.2 canonical smoke

见 §10.2。`discovered=3`，`loaded=3`，`lossless_check=...:ok`。

---

## 12. 明确未做（越界即打回）

```text
A2 document_title / source / registry
A3 heading split / chunk / page_start / page_end
A4 hierarchy
A5 embedding
A6 Milvus / rebuild / upsert
PDF / OCR / table reparse / caption 修复 / 正文 rewrite
LangChain Document
旧 C8 DataPreparationModule 复用
按路径名过滤 acceptance/smoke
按目录名 / hash 后缀做 corpus dedup
packaging / setup.py / pyproject
```

---

## 13. 关闭条件（服务器已满足，申请 CLOSED）

```text
A1 DESIGN                 PASS
A1 IMPLEMENTATION         PASS（enumeration OSError 已包装）
A1 UNIT TEST              PASS（Linux 16 passed / 0 skipped）
A1 REAL CORPUS            PASS（3 hash identities；discovered=loaded=3）
A1 CLOSED                 READY FOR UPSTREAM CLOSE
```

原先三条关闭条件对照：

1. Linux pytest `16 passed, 0 skipped`，T8 真实 PASSED。**已满足。**
2. 正式根每个 source 一份 A0 发布；无 legacy `01_nasa_materials_aluminum_2014/`。**已满足。**
3. 对该根 smoke：`discovered == loaded`，且不再同时出现无-hash / hash 铝手册。**已满足。**

A2 仍不得清洗 A0 `document.md`。当前 3 篇是抽查规模 canonical；全量建库是后续 corpus 追加，不回退 A1。

