# A0 冻结补记（2026-08-15）

本页覆盖 `ver0.1-preprocessing.md` 里仍把「36–48 页矩阵必须全部 accepted」写成 P0-2 的旧合同。
现行验收以本文与 `docs/preprocessing.md`、`verify_pdf_preprocess_server.py` 为准。

## 冻结内容

1. Task 1 Region-OCR Visual Isolation、Task 2 空单元格、Task 2.5 AABB 外污染已落地。
2. **嵌套表、以及格子归属为 0 或 ≥2 的表 = 结构不可信。** V0.1 不收表体，只留可信表注。A1 不得再洗。
3. 方向 `top1≥0.90`、中心裁剪墨量、`alphanumeric_ratio≥0.35` 不调参。
4. 5006A Golden：36–46 与 48 必须有 accepted matrix；**第 47 页必须 `rejected` 且不进入 Markdown**。

## Golden 改动

`validate_5006a_golden_artifacts()` 不再把 `[47]` 当成失败。第 47 页若被收成 accepted matrix，或缺少 native 拒绝产物，Golden 失败。

服务器同步后：

```bash
export PYTHONPATH="$PWD/docling:$PWD/code/preprocessing${PYTHONPATH:+:$PYTHONPATH}"
python -m pytest code/preprocessing/tests -v
# 然后按 docs/Preprocessing/2026-08-14-region-ocr-isolation-and-native-table-empty-cell.md 6.2 重跑 Golden
# 期望 final_acceptance_exit_code=0
```

## 不阻塞 A0 的后续项

- 24 份语料全量 sanity
- 扫描 OCR 准确率小样本（原 P1）
- 嵌套表还原（若做，必须另开几何通解，禁止 NASA/页码/栏名特判）
